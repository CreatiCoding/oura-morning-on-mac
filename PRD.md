# PRD: Oura 링 기반 수면 충족 기상 알람 (가칭 "WakeReady")

- 문서 버전: v0.1 (초안)
- 작성일: 2026-09-05
- 대상: 개인 프로젝트 (단일 사용자)

---

## 1. 개요 (Overview)

### 1.1 한 줄 정의

자는 동안 Oura 링(5세대)의 데이터를 실시간에 가깝게 직접 읽어, **"충분히 잤다"는 조건이 충족되는 순간 사용자를 깨워주는** 맥미니 상주형 알람 시스템.

### 1.2 문제 정의

- 기존 알람은 수면의 질/양과 무관하게 고정 시각에 울린다.
- Oura 공식 앱과 공식 API는 실시간 기상 판정을 지원하지 않는다.
  - 데이터 경로가 `링 → 폰 앱 동기화 → 클라우드 → API`라서, 자는 도중에는 데이터가 API에 도달하지 않는다.
  - 수면 세션 데이터는 기상 후 동기화·분석이 끝나야 조회 가능하다.
- 따라서 "잘 잤으면 이제 일어나도 된다"를 판정하려면 클라우드를 우회해 **링과 직접 BLE 통신**해야 한다.

### 1.3 솔루션 요약

- 리버스 엔지니어링 프로젝트 **open_oura** (Rust 툴킷 + Python 연구 벤치, Ring 3/4/5 검증됨)를 활용한다.
- 상시 전원이 켜져 있는 **맥미니**가 밤 동안 링과 BLE 연결을 점유하고, 주기적으로 수면 이벤트를 읽는다.
- 수면 충족 조건이 만족되면 알람을 발동하고, 링 연결을 해제해 아이폰 Oura 앱의 정상 동기화를 복원한다.

---

## 2. 목표 및 비목표

### 2.1 목표 (Goals)

| # | 목표 | 성공 기준 |
|---|------|-----------|
| G1 | 수면 충족 여부를 밤중에 판정 | 총 수면 시간 ≥ 목표치(기본 8h) 판정 동작 |
| G2 | 조건 충족 시 능동적으로 깨움 | 판정 후 5분 이내 알람 발동 |
| G3 | 공식 앱 경험 보존 | 아침 해제 후 Oura 앱 동기화 정상 (수면 점수/Readiness 산출) |
| G4 | 무인 운영 | 사용자가 매일 아무 조작 없이 자동 시작/종료 |

### 2.2 확장 목표 (Stretch)

- REM 수면 시간 기준 판정 (예: REM ≥ 90분)
- 수면 단계 기반 "얕은 수면 구간에서 깨우기" (스마트 알람)
- HomeKit 조명 연동 (서서히 밝아지는 기상)

### 2.3 비목표 (Non-Goals)

- 다중 사용자 지원, 배포/상용화 (개인 사용 전용)
- iOS 네이티브 앱 개발 (v1에서는 제외, 향후 검토)
- Oura 공식 API / 클라우드 연동 (본 프로젝트의 목적과 불일치)
- 링 설정 변경·펌웨어 조작 등 상태 변경 명령 사용 (읽기 전용 원칙)

---

## 3. 사용자 시나리오

### 3.1 핵심 시나리오 (Happy Path)

1. 사용자는 평소처럼 링을 끼고 잔다. (별도 조작 없음)
2. 밤 시간대(예: 23:00)에 맥미니의 launchd가 세션 스크립트를 자동 시작한다.
3. 스크립트가 링과 BLE 연결 후, N분 간격으로 수면 이벤트를 폴링한다.
4. 누적 수면 시간이 목표(8h)에 도달하면:
   - 알람 발동 (맥미니 스피커 + 폰 푸시)
   - 링 연결 즉시 해제
5. 사용자가 기상, 아이폰 Oura 앱이 자동 재연결·동기화. 공식 앱의 수면 점수는 평소처럼 표시된다.

### 3.2 예외 시나리오

| 상황 | 시스템 동작 |
|------|-------------|
| 목표 시각까지 수면 미충족 | 안전 상한 시각(예: 09:00)에 무조건 알람 + 세션 종료 |
| 밤중 BLE 연결 유실 | 자동 재연결 시도 (백오프), 실패 누적 시 상한 시각 알람으로 폴백 |
| 스크립트 크래시 | macOS가 BLE 연결 회수 → 폰 동기화는 정상. launchd 재시작 정책 적용 |
| 링 배터리 부족 | 세션 시작 시 배터리 확인, 임계치 미만이면 경고 알림 후 폴백 모드 |
| 펌웨어 업데이트로 프로토콜 변경 | 세션 실패를 감지하고 폴백 알람으로 동작 (링/앱은 무손상) |

---

## 4. 기능 요구사항

### FR-1. 링 연결 관리
- FR-1.1 지정 시각에 링 스캔 → 연결 → 인증(앱 레벨 AES 핸드셰이크)을 수행한다.
- FR-1.2 연결 해제는 코드로 명시 제어한다. (알람 발동 시 / 상한 시각 도달 시 / 종료 시)
- FR-1.3 읽기 전용 명령만 사용한다. open_oura의 `--include-state`, `--include-danger` 계열 명령은 금지한다.

### FR-2. 수면 데이터 수집
- FR-2.1 N분(기본 10분) 간격으로 이벤트 스트림(GetEvent/ExtGetEvent)을 폴링한다.
- FR-2.2 수면 시작 시각을 추정한다. (온디바이스 수면 단계 이벤트 및 심박/모션 기반)
- FR-2.3 수집 원본은 JSONL로 로컬 저장한다. (검증·디버깅·로직 개선용)

### FR-3. 기상 판정 로직
- FR-3.1 기본 규칙: `누적 수면 시간 ≥ 목표 수면 시간(기본 8h)` → 기상 조건 충족.
- FR-3.2 목표 수면 시간은 설정 파일로 조정 가능하다.
- FR-3.3 (Stretch) REM 누적 시간 조건을 AND/OR로 결합 가능하다.
- FR-3.4 안전 상한 시각(기본 09:00) 도달 시 조건과 무관하게 알람을 발동한다.

### FR-4. 알람 발동
- FR-4.1 맥미니 로컬 사운드 재생 (`afplay`, 볼륨 강제 설정 포함).
- FR-4.2 아이폰 푸시 알림 (ntfy.sh 또는 Pushover, 방해금지 우회 가능한 우선순위 사용).
- FR-4.3 알람 발동 직후 FR-1.2에 따라 링 연결을 해제한다.

### FR-5. 스케줄링 및 상주화
- FR-5.1 launchd로 매일 지정 시각에 세션을 자동 시작한다.
- FR-5.2 세션은 알람 발동 또는 상한 시각에 반드시 종료된다. (3중 안전장치: 알람 시 해제 → 상한 시각 강제 종료 → 프로세스 종료 시 OS의 연결 회수)
- FR-5.3 세션 로그를 남기고, 실패 시 다음 날 정상 재시도한다.

---

## 5. 비기능 요구사항

| 항목 | 요구사항 |
|------|----------|
| 신뢰성 | 어떤 실패 상황에서도 "알람이 안 울리는" 경우가 없어야 함 (폴백 알람 필수) |
| 안전성 | 링에 쓰기/상태 변경 명령 금지. 공식 앱 데이터·페어링 무손상 보장 |
| 복구성 | 시스템 이상 시 수동 복구는 "맥미니 블루투스 off/on 또는 프로세스 kill"로 충분해야 함 |
| 프라이버시 | 모든 데이터는 맥미니 로컬에만 저장. 외부 전송은 푸시 알림 트리거뿐 |
| 전력/자원 | 맥미니 상시 구동 전제. 폴링 간격은 링 배터리 영향을 고려해 조정 가능 |

---

## 6. 시스템 아키텍처

```
[밤 23:00~]                          [아침]
Oura Ring 5 ←─BLE(읽기 전용)─→ 맥미니          아이폰 Oura 앱
     │                            │                  │
     │  ① 연결/인증               │                  │
     │  ② 10분 간격 이벤트 폴링    │                  │
     │  ③ 수면 판정               │                  │
     │  ④ 조건 충족 → 알람 ──────→ 스피커/푸시        │
     │  ⑤ disconnect ────────────────────────────→ 자동 재연결
     │                                              & 동기화
```

- 언어/도구: open_oura (Rust CLI 또는 Python 벤치) + 판정/알람 래퍼 스크립트(Python)
- 저장: JSONL 이벤트 로그 + SQLite(oura-store 활용 가능)
- 스케줄러: macOS launchd

---

## 7. 주요 리스크 및 대응

| 리스크 | 심각도 | 대응 |
|--------|--------|------|
| 비공식 프로토콜 → 펌웨어 업데이트로 파손 가능 | 중 | 폴백 알람으로 기상 실패 방지. 프로토콜 수정은 open_oura 업스트림 추적 |
| BLE 연결 경합 (폰이 먼저 연결 점유) | 중 | 세션 시작 시 재시도 로직. 취침 전 폰과의 근접성 고려한 시작 시각 조정 |
| 수면 시작 시각 추정 오차 | 중 | 초기 2주는 로그 수집·검증 기간으로 운영, 판정 로직 보정 |
| 폴링이 링 배터리에 주는 영향 미확인 | 낮 | 폴링 간격 파라미터화, 배터리 로그로 영향 측정 |
| iOS 앱 재연결 지연 | 낮 | 기능 영향 없음. 필요 시 앱 1회 실행으로 즉시 동기화 |
| ToS 위반 소지 | 낮 | 개인 사용 한정, 클라우드 미접촉, 읽기 전용 운영 |

---

## 8. 마일스톤

| 단계 | 내용 | 완료 기준 |
|------|------|-----------|
| M0 | 환경 구축 | 맥미니에서 open_oura 실행, 터미널 BLE 권한 부여, 링 스캔 성공 |
| M1 | 연결·인증 검증 | Ring 5 연결 + 인증 통과, 배터리/실시간 심박 읽기 성공 |
| M2 | 야간 데이터 수집 | 하룻밤 이벤트 스트림 JSONL 확보, 수면 이벤트 형태 분석 |
| M3 | 판정 로직 | 로그 기반 수면 시작 감지 + 누적 시간 계산 정확도 검증 (±15분 이내) |
| M4 | 알람 + 해제 흐름 | 판정 → 알람 → disconnect → 폰 자동 동기화 전체 흐름 1회 성공 |
| M5 | 상주화 | launchd 등록, 3중 안전장치 동작 확인, 7일 연속 무인 운영 성공 |
| M6 (Stretch) | REM 조건 / 스마트 기상 / HomeKit | 각 기능 단위 검증 |

---

## 9. 미해결 질문 (Open Questions)

1. Ring 5의 수면 단계 이벤트가 **어느 주기·지연으로** 이벤트 버퍼에 기록되는가? (M2에서 실측 필요 — 판정 정밀도의 핵심 변수)
2. 밤중 장시간 연결 점유 시 링의 광고/절전 동작에 부작용이 없는가? (M2~M3 관찰)
3. 폴링 대신 실시간 리스너(`oura_realtime_listener.py`) 상시 구독이 배터리·안정성 면에서 더 나은가? (M2에서 비교)
4. 수면 시작 감지를 링 이벤트만으로 할지, "평소 취침 시각 + 심박 하강 패턴" 보조 추정을 둘지? (M3에서 결정)
5. 알람 수단의 최종 구성: 맥미니 스피커 단독으로 충분한가, 폰 푸시 병행이 필요한가? (M4 실사용 후 결정)

---

## 10. 용어

| 용어 | 설명 |
|------|------|
| open_oura | Oura 링 BLE 프로토콜을 리버스 엔지니어링한 오픈소스 (Rust+Python), Ring 3/4/5 검증 |
| GetEvent / ExtGetEvent | 링 내부 이벤트 버퍼를 읽는 프로토콜 명령 (수면 단계·HR·모션 등 포함) |
| 세션 | 밤 시작~아침 종료까지의 1회 실행 단위 |
| 폴백 알람 | 판정 실패·시스템 이상 시에도 반드시 울리는 안전 상한 시각 알람 |
